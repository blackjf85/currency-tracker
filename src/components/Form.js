import React from 'react'

const Form = ({ onChange, onSubmit}) => {
    return (
        <div>
            <form onSubmit={onSubmit}>
            
            </form>
        </div>
    )
}

export default Form
